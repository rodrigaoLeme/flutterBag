import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primária
  static const Color primary = Color(0xFF244777);
  static const Color primaryLight = Color(0xFF3D8DD4);
  static const Color primaryDark = Color(0xFF003B7A);

  // Secundária
  static const Color secondary = Color(0xFFE0E2EC);
  static const Color secondaryLight = Color(0xFFE0E2EC);
  static const Color secondaryDark = Color(0xFFE0E2EC);

  // Erro e status
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF0288D1);

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF121212);

  // Superfícies
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Texto
  static const Color textPrimaryLight = Color(0xFF43474E);
  static const Color textSecondaryLight = Color(0xFF191C20);
  static const Color textPrimaryDark = Color(0xFFEEEEEE);
  static const Color textSecondaryDark = Color(0xFF9E9E9E);

  // Input
  static const Color inputFillLight = Color(0xFFFFFFFF);
  static const Color inputFillDark = Color(0xFF2C2C2C);

  // Bordas
  static const Color borderLight = Color(0xFFBDBDBD);
  static const Color borderDark = Color(0xFF424242);

  // Divisor
  static const Color dividerLight = Color(0xFFE0E0E0);
  static const Color dividerDark = Color(0xFF303030);
}
